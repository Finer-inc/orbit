# Seirei 技術構成概略図

```
                          ┌─────────────────────────┐
                          │     External Services    │
                          ├─────────────────────────┤
                          │  Anthropic API (Haiku)   │
                          │  X (Twitter) OAuth       │
                          │  Supabase (DB)           │
                          └──────┬──────────────────┘
                                 │ HTTPS
                                 │
┌────────────────────────────────┼──────────────────────────────┐
│                     Go Process (spirits/)                      │
│                                │                              │
│  ┌─────────────────────────────┴───────────────────────────┐  │
│  │              Spirit Agent (PicoClaw components)          │  │
│  │                                                         │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │  │
│  │  │ Agent    │ │Anthropic │ │ Tool     │ │ Session  │   │  │
│  │  │ Loop     │ │ Provider │ │ Registry │ │ Manager  │   │  │
│  │  │ (custom) │ │ (custom) │ │(PicoClaw)│ │(PicoClaw)│   │  │
│  │  └────┬─────┘ └──────────┘ └────┬─────┘ └──────────┘   │  │
│  │       │                         │                       │  │
│  │       │    ┌────────────────────┘                       │  │
│  │       │    │  Spirit Tools                              │  │
│  │       │    │  observe / move_to / talk_to / think       │  │
│  │       │    │                                            │  │
│  └───────┼────┼────────────────────────────────────────────┘  │
│          │    │                                                │
└──────────┼────┼────────────────────────────────────────────────┘
           │    │
           │    │ HTTP localhost:3001
           │    │
┌──────────┼────┼────────────────────────────────────────────────┐
│          │    │    TS Process (server/api.ts)                   │
│  ┌───────┴────┴────────────────────────────────────────────┐  │
│  │                  World Server (Hono)                      │  │
│  │                                                          │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐  │  │
│  │  │位置管理   │ │視界計算   │ │遭遇判定   │ │時間システム │  │  │
│  │  │ x,y,z    │ │ Frustum  │ │ 距離     │ │ 朝昼夕夜   │  │  │
│  │  │ BBox     │ │ Culling  │ │ チェック  │ │ サイクル   │  │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └────────────┘  │  │
│  └──────────────────────────┬───────────────────────────────┘  │
│                              │                                 │
└──────────────────────────────┼─────────────────────────────────┘
                               │
                               │ WebSocket / REST (未実装)
                               │
┌──────────────────────────────┼─────────────────────────────────┐
│                       Browser (TypeScript)                      │
│  ┌───────────────────────────┴─────────────────────────────┐   │
│  │              React 19 + Three.js + R3F (実装済)          │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────┘
```

## 精霊1体の思考サイクル

```
┌─────────────────────────────────────────────────────────────┐
│                 Go Process: PicoClaw AgentLoop                │
│                                                               │
│  ┌──────┐    ┌──────────┐    ┌──────┐    ┌───────┐          │
│  │      │    │ Context  │    │      │    │       │          │
│  │ 記憶 ├───→│ 構築     ├───→│ LLM  ├───→│ Tool  │          │
│  │ 読込 │    │ 人格+記憶│    │ 呼出 │    │ 実行  │          │
│  │      │    │ +観察結果│    │      │    │       │          │
│  └──────┘    └──────────┘    └──┬───┘    └───┬───┘          │
│                                 │            │               │
│                                 │  ツール結果  │               │
│                                 │←───────────┘               │
│                                 │                             │
│                           完了判定                             │
│                           ├─ ツール呼出あり → 繰返し           │
│                           └─ テキスト応答 → 完了               │
│                                 │                             │
│  ┌──────┐    ┌──────────┐       │                             │
│  │      │    │ Context  │       │                             │
│  │ 記憶 │←──┤ 圧縮     │←──────┘                             │
│  │ 保存 │    │ (自動)   │                                     │
│  │      │    │          │                                     │
│  └──────┘    └──────────┘                                     │
│                                                               │
│  ※ observe / move_to は HTTP で TS World Server を呼び出す    │
│  ※ think / remember は Go プロセス内で完結                     │
└─────────────────────────────────────────────────────────────┘
          │                                     ▲
          │ HTTP (observe, move_to, talk_to)     │ レスポンス
          ▼                                     │
┌─────────────────────────────────────────────────────────────┐
│              TS Process: World Server (localhost:3001)        │
│                                                               │
│  observe  → 視界内オブジェクト + 近くの精霊を返す              │
│  move_to  → 位置更新して結果を返す                             │
│  talk_to  → 相手への発話を中継                                 │
└─────────────────────────────────────────────────────────────┘

         ↓ スケジューラが次の思考時刻まで待機 ↓
```

## データフロー

```
User (Browser)
  │
  │ 画面を開く
  ▼
Frontend ──WebSocket/REST (未実装)──→ TS World Server (localhost:3001)
                                       │
                                       ├─→ 精霊の位置・状態を配信
                                       ├─→ 会話イベントを配信
                                       └─→ 時間帯変更を配信

Spirit (Go goroutine)
  │
  │ 定期的にtick
  ▼
AgentLoop (Go Process)
  │
  │                        HTTP localhost:3001
  ├─ observe ─────────────────→ TS World Server ──→ 視界内オブジェクト + 近くの精霊
  ├─ move_to ─────────────────→ TS World Server ──→ 位置更新 ──→ WebSocketで配信
  ├─ talk_to ─────────────────→ TS World Server ──→ 相手に受信 + ログ保存
  │
  │                        Go Process内で完結
  ├─ think   ──→ Session (Go)   ──→ 記憶に保存
  ├─ report  ──→ Supabase       ──→ 持ち主に通知
  └─ remember──→ Supabase       ──→ 長期記憶に保存
```
